) +
ggplot2::theme_bw() +
ggplot2::theme(
axis.text.x = ggplot2::element_text(angle = 45, hjust = 1)
)
}
# let´s test it
ranked_barplot(mpg, "manufacturer")
library(ggplot2)
# let´s test it
ranked_barplot(mpg, "manufacturer")
ranked_barplot(mpg, "manufacturer", scale = "percent")
usethis::use_r("ranked_barplot")
document()
#' @param var Character. Name of the nominal variable in \code{df}.
#' @param connect Logical; if TRUE, connects points with a line.
#'
#' @return A \code{ggplot2} object.
#'
#' @examples
#' ranked_dotplot(categories, "animal")
#' ranked_dotplot(categories, "animal", connect = TRUE)
#'
#' @export
ranked_dotplot <- function(df, var, connect = FALSE) {
# ---- Input checks ----
if (!is.data.frame(df)) {
stop("`df` must be a data.frame or tibble.")
}
if (!is.character(var) || length(var) != 1) {
stop("`var` must be a single character string.")
}
if (!var %in% names(df)) {
stop("`var` must be a column name in `df`.")
}
if (is.list(df[[var]])) {
stop("`var` must refer to an atomic (non-list) column.")
}
# ---- Frequency table ----
freq_table <- as.data.frame(
table(df[[var]]),
stringsAsFactors = FALSE
)
names(freq_table) <- c("Category", "Freq")
if (nrow(freq_table) < 2) {
stop("The variable must have at least two categories.")
}
# ---- Order categories ----
freq_table <- freq_table[order(-freq_table$Freq), ]
freq_table$Category <- factor(
freq_table$Category,
levels = freq_table$Category
)
# ---- Plot ----
p <- ggplot2::ggplot(
freq_table,
ggplot2::aes(x = Category, y = Freq, group = 1)
) +
ggplot2::geom_point(size = 3) +
ggplot2::labs(
x = var,
y = "Frequency"
) +
ggplot2::theme_bw() +
ggplot2::theme(
axis.text.x = ggplot2::element_text(angle = 45, hjust = 1)
)
if (connect) {
p <- p + ggplot2::geom_line()
}
return(p)
}
ranked_dotplot(mpg, "manufacturer")
ranked_dotplot(mpg, "manufacturer", connect = TRUE)
ranked_dotplot(mpg, "manufacturer", connect = TRUE,scale = "percent")
#' @param scale Character; either \code{"count"} (default) or \code{"percent"}.
#'
#' @return A \code{ggplot2} object.
#'
#' @examples
#' ranked_dotplot(categories, "animal")
#' ranked_dotplot(categories, "animal", connect = TRUE)
#' ranked_dotplot(mpg, "manufacturer", scale = "percent")
#'
#' @export
ranked_dotplot <- function(df,
var,
connect = FALSE,
scale = c("count", "percent")) {
# ---- Input checks ----
if (!is.data.frame(df)) {
stop("`df` must be a data.frame or tibble.")
}
if (!is.character(var) || length(var) != 1) {
stop("`var` must be a single character string.")
}
if (!var %in% names(df)) {
stop("`var` must be a column name in `df`.")
}
if (is.list(df[[var]])) {
stop("`var` must refer to an atomic (non-list) column.")
}
scale <- match.arg(scale)
# ---- Frequency table ----
freq_table <- as.data.frame(
table(df[[var]]),
stringsAsFactors = FALSE
)
names(freq_table) <- c("Category", "Freq")
if (nrow(freq_table) < 2) {
stop("The variable must have at least two categories.")
}
# ---- Optional scaling ----
if (scale == "percent") {
freq_table$Freq <- freq_table$Freq / sum(freq_table$Freq) * 100
}
# ---- Order categories ----
freq_table <- freq_table[order(-freq_table$Freq), ]
freq_table$Category <- factor(
freq_table$Category,
levels = freq_table$Category
)
# ---- Plot ----
p <- ggplot2::ggplot(
freq_table,
ggplot2::aes(x = Category, y = Freq, group = 1)
) +
ggplot2::geom_point(size = 3) +
ggplot2::labs(
x = var,
y = ifelse(scale == "percent", "Percentage", "Frequency")
) +
ggplot2::theme_bw() +
ggplot2::theme(
axis.text.x = ggplot2::element_text(angle = 45, hjust = 1)
)
if (connect) {
p <- p + ggplot2::geom_line()
}
return(p)
}
ranked_dotplot(mpg, "manufacturer")
ranked_dotplot(mpg, "manufacturer", connect = TRUE)
ranked_dotplot(mpg, "manufacturer", connect = TRUE,scale = "percent")
ranked_dotplot(mpg, "manufacturer")
ranked_dotplot(mpg, "manufacturer", connect = TRUE)
ranked_dotplot(mpg, "manufacturer", connect = TRUE,scale = "percent")
usethis::use_r("ranked_dotplot")
document()
load_all()
rm(list = c("ranked_barplot", "ranked_dotplot"))
load_all()
document()
#' @param scale Character; either \code{"count"} (default) or \code{"percent"}.
#'
#' @return A \code{ggplot2} object.
#'
#' @examples
#' centered_dotplot(categories, "animal")
#' centered_dotplot(categories, "animal", connect = TRUE)
#' centered_dotplot(mpg, "manufacturer", scale = "percent")
#'
#' @export
centered_dotplot <- function(df,
var,
connect = FALSE,
scale = c("count", "percent")) {
# ---- Input checks ----
if (!is.data.frame(df)) {
stop("`df` must be a data.frame or tibble.")
}
if (!is.character(var) || length(var) != 1) {
stop("`var` must be a single character string.")
}
if (!var %in% names(df)) {
stop("`var` must be a column name in `df`.")
}
if (is.list(df[[var]])) {
stop("`var` must refer to an atomic (non-list) column.")
}
scale <- match.arg(scale)
# ---- Frequency table ----
freq_table <- as.data.frame(
table(df[[var]]),
stringsAsFactors = FALSE
)
names(freq_table) <- c("Category", "Freq")
if (nrow(freq_table) < 2) {
stop("The variable must have at least two categories.")
}
# ---- Optional scaling ----
if (scale == "percent") {
freq_table$Freq <- freq_table$Freq / sum(freq_table$Freq) * 100
}
# ---- Sort by frequency (descending) ----
freq_table <- freq_table[order(-freq_table$Freq), ]
# ---- Centered positioning algorithm ----
f <- nrow(freq_table)
center <- ceiling(f / 2)
positions <- integer(f)
positions[1] <- center
right <- center
left <- center
side <- 1
for (i in 2:f) {
if (side == 1) {
right <- right + 1
positions[i] <- right
} else {
left <- left - 1
positions[i] <- left
}
side <- -side
}
freq_table$PlotPos <- positions
freq_table <- freq_table[order(freq_table$PlotPos), ]
freq_table$Category <- factor(
freq_table$Category,
levels = freq_table$Category
)
# ---- Plot ----
p <- ggplot2::ggplot(
freq_table,
ggplot2::aes(x = Category, y = Freq, group = 1)
) +
ggplot2::geom_point(size = 3) +
ggplot2::labs(
x = var,
y = ifelse(scale == "percent", "Percentage", "Frequency")
) +
ggplot2::theme_bw() +
ggplot2::theme(
axis.text.x = ggplot2::element_text(angle = 45, hjust = 1)
)
if (connect) {
p <- p + ggplot2::geom_line()
}
return(p)
}
centered_dotplot(mpg, "manufacturer")
centered_dotplot(mpg, "manufacturer", connect = TRUE)
centered_dotplot(mpg, "manufacturer", connect = TRUE,scale = "percent")
usethis::use_r("centered_dotplot")
document()
build_vignettes()
use_vignette(
name  = "centered_dotplot",
title = "Centered Dot Plots for Nominal Variables"
)
use_vignette(  name  = "ranked_barplot",
title = "Ranked Bar Plots for Nominal Variables")
use_vignette(name  = "ranked_dotplot",
title = "Ranked Dot Plots for Nominal Variables")
document()
rm(list = c("centered_dotplot"))
document()
usethis::use_vignette(
name  = "nominal_distribution_shapes",
title = "Visualizing and Analyzing Distributions of Nominal Variables"
)
library(nomiShape)
load_all()
ranked_barplot(categories, "animal")
ranked_dotplot(categories, "animal")
# Example usage of centered_barplot
centered_barplot(categories, "animal", scale = "percent")
# Example usage of centered_barplot
centered_barplot(categories, "animal", scale = "percent")
# Example usage of ranked_barplot
ranked_barplot(categories, "animal")
# Example usage of ranked_dotplot
ranked_dotplot(categories, "animal")
# Example usage of ranked_dotplot
ranked_dotplot(categories, "animal", connect = TRUE)
# Example usage of pareto_chart
pareto_chart(categories, "animal")
pareto_chart(categories, "animal")
# Example usage of pareto
pareto(categories, "animal")
# Example usage of centered_barplot
centered_barplot(categories, "animal", scale = "percent")
# Example usage of centered_dotplot
centered_dotplot(categories, "animal")
# Example usage of centered_dotplot
centered_dotplot(categories, "animal",connect = TRUE)
# Example usage of centered_barplot
centered_barplot(categories, "animal")
#'
#' @return A \code{ggplot2} object.
#'
#' @examples
#' centered_dotplot(categories, "animal")
#' centered_dotplot(categories, "animal", connect = TRUE)
#' centered_dotplot(categories, "animal", connect = TRUE, shade = TRUE)
#' centered_dotplot(mpg, "manufacturer", scale = "percent")
#'
#' @export
centered_dotplot <- function(df,
var,
connect = FALSE,
shade = FALSE,
scale = c("count", "percent")) {
# ---- Input checks ----
if (!is.data.frame(df)) stop("`df` must be a data.frame or tibble.")
if (!is.character(var) || length(var) != 1) stop("`var` must be a single character string.")
if (!var %in% names(df)) stop("`var` must be a column name in `df`.")
if (is.list(df[[var]])) stop("`var` must refer to an atomic (non-list) column.")
scale <- match.arg(scale)
# ---- Frequency table ----
freq_table <- as.data.frame(table(df[[var]]), stringsAsFactors = FALSE)
names(freq_table) <- c("Category", "Freq")
if (nrow(freq_table) < 2) stop("The variable must have at least two categories.")
# ---- Optional scaling ----
if (scale == "percent") freq_table$Freq <- freq_table$Freq / sum(freq_table$Freq) * 100
# ---- Sort by frequency (descending) ----
freq_table <- freq_table[order(-freq_table$Freq), ]
# ---- Centered positioning algorithm ----
f <- nrow(freq_table)
center <- ceiling(f / 2)
positions <- integer(f)
positions[1] <- center
right <- center
left <- center
side <- 1
for (i in 2:f) {
if (side == 1) {
right <- right + 1
positions[i] <- right
} else {
left <- left - 1
positions[i] <- left
}
side <- -side
}
freq_table$PlotPos <- positions
freq_table <- freq_table[order(freq_table$PlotPos), ]
freq_table$Category <- factor(freq_table$Category, levels = freq_table$Category)
# ---- Plot ----
p <- ggplot2::ggplot(freq_table, ggplot2::aes(x = Category, y = Freq, group = 1)) +
ggplot2::geom_point(size = 3) +
ggplot2::labs(x = var, y = ifelse(scale == "percent", "Percentage", "Frequency")) +
ggplot2::theme_bw() +
ggplot2::theme(axis.text.x = ggplot2::element_text(angle = 45, hjust = 1))
if (connect) {
p <- p + ggplot2::geom_line()
if (shade) {
p <- p + ggplot2::geom_ribbon(aes(ymin = 0, ymax = Freq), fill = "grey80", alpha = 0.5)
}
}
return(p)
}
centered_dotplot(categories, "animal", connect = TRUE, shade = TRUE)
centered_dotplot(categories, "animal", connect = TRUE, shade = TRUE, scale = "percent")
usethis::use_r("centered_dotplot")
document()
rm(list = c("centered_dotplot"))
document()
library(nomiShape)
# Example usage of ranked_barplot
ranked_barplot(categories, "animal")
# Example usage of ranked_dotplot
ranked_dotplot(categories, "animal", connect = TRUE)
# Example usage of pareto
pareto(categories, "animal")
# Example usage of centered_barplot
centered_barplot(categories, "animal")
# Example usage of centered_dotplot
centered_dotplot(categories, "animal",connect = TRUE)
# Example usage of centered_dotplot
centered_dotplot(categories, "animal",connect = TRUE,shade=TRUE)
# Example usage of centered_dotplot
centered_dotplot(categories, "animal",connect = TRUE)
#'
#' @return A \code{ggplot2} object.
#'
#' @examples
#' centered_dotplot(categories, "animal")
#' centered_dotplot(categories, "animal", connect = TRUE)
#' centered_dotplot(categories, "animal", connect = TRUE, shade = TRUE)
#' centered_dotplot(mpg, "manufacturer", scale = "percent")
#'
#' @export
centered_dotplot <- function(df,
var,
connect = FALSE,
shade = FALSE,
scale = c("count", "percent")) {
# ---- Input checks ----
if (!is.data.frame(df)) stop("`df` must be a data.frame or tibble.")
if (!is.character(var) || length(var) != 1) stop("`var` must be a single character string.")
if (!var %in% names(df)) stop("`var` must be a column name in `df`.")
if (is.list(df[[var]])) stop("`var` must refer to an atomic (non-list) column.")
scale <- match.arg(scale)
# ---- Frequency table ----
freq_table <- as.data.frame(table(df[[var]]), stringsAsFactors = FALSE)
names(freq_table) <- c("Category", "Freq")
if (nrow(freq_table) < 2) stop("The variable must have at least two categories.")
# ---- Optional scaling ----
if (scale == "percent") freq_table$Freq <- freq_table$Freq / sum(freq_table$Freq) * 100
# ---- Sort by frequency (descending) ----
freq_table <- freq_table[order(-freq_table$Freq), ]
# ---- Centered positioning algorithm ----
f <- nrow(freq_table)
center <- ceiling(f / 2)
positions <- integer(f)
positions[1] <- center
right <- center
left <- center
side <- 1
for (i in 2:f) {
if (side == 1) {
right <- right + 1
positions[i] <- right
} else {
left <- left - 1
positions[i] <- left
}
side <- -side
}
freq_table$PlotPos <- positions
freq_table <- freq_table[order(freq_table$PlotPos), ]
freq_table$Category <- factor(freq_table$Category, levels = freq_table$Category)
# ---- Plot ----
p <- ggplot2::ggplot(freq_table, ggplot2::aes(x = Category, y = Freq, group = 1)) +
ggplot2::geom_point(size = 3) +
ggplot2::labs(x = var, y = ifelse(scale == "percent", "Percentage", "Frequency")) +
ggplot2::theme_bw() +
ggplot2::theme(axis.text.x = ggplot2::element_text(angle = 45, hjust = 1))
if (connect) {
p <- p + ggplot2::geom_line()
if (shade) {
ymin_val <- min(freq_table$Freq)  # <- key change
p <- p + ggplot2::geom_ribbon(aes(ymin = ymin_val, ymax = Freq), fill = "grey80", alpha = 0.5)
}
}
return(p)
}
# Example usage of centered_dotplot
centered_dotplot(categories, "animal",connect = TRUE)
# Example usage of centered_dotplot
centered_dotplot(categories, "animal",connect = TRUE,shade = TRUE)
document()
rm(list = c("centered_dotplot"))
document()
library(nomiShape)
# Example usage of ranked_dotplot
ranked_dotplot(categories, "animal")
# Example with connecting line
ranked_dotplot(categories, "animal", connect = TRUE)
# Example with percent scale
ranked_dotplot(mpg, "manufacturer", scale = "percent")
#'
#' @return A \code{ggplot2} object.
#'
#' @examples
#' ranked_dotplot(categories, "animal")
#' ranked_dotplot(categories, "animal", connect = TRUE)
#' ranked_dotplot(categories, "animal", connect = TRUE, shade = TRUE)
#' ranked_dotplot(mpg, "manufacturer", scale = "percent")
#'
#' @export
ranked_dotplot <- function(df,
var,
connect = FALSE,
shade = FALSE,
scale = c("count", "percent")) {
# ---- Input checks ----
if (!is.data.frame(df)) stop("`df` must be a data.frame or tibble.")
if (!is.character(var) || length(var) != 1) stop("`var` must be a single character string.")
if (!var %in% names(df)) stop("`var` must be a column name in `df`.")
if (is.list(df[[var]])) stop("`var` must refer to an atomic (non-list) column.")
scale <- match.arg(scale)
# ---- Frequency table ----
freq_table <- as.data.frame(table(df[[var]]), stringsAsFactors = FALSE)
names(freq_table) <- c("Category", "Freq")
if (nrow(freq_table) < 2) stop("The variable must have at least two categories.")
# ---- Optional scaling ----
if (scale == "percent") {
freq_table$Freq <- freq_table$Freq / sum(freq_table$Freq) * 100
}
# ---- Order categories by frequency ----
freq_table <- freq_table[order(-freq_table$Freq), ]
freq_table$Category <- factor(freq_table$Category, levels = freq_table$Category)
# ---- Plot ----
p <- ggplot2::ggplot(freq_table, ggplot2::aes(x = Category, y = Freq, group = 1)) +
ggplot2::geom_point(size = 3) +
ggplot2::labs(x = var, y = ifelse(scale == "percent", "Percentage", "Frequency")) +
ggplot2::theme_bw() +
ggplot2::theme(axis.text.x = ggplot2::element_text(angle = 45, hjust = 1))
if (connect) p <- p + ggplot2::geom_line()
if (shade) {
ymin_val <- min(freq_table$Freq)
p <- p + ggplot2::geom_ribbon(aes(ymin = ymin_val, ymax = Freq), fill = "grey80", alpha = 0.5)
}
return(p)
}
ranked_dotplot(mpg, "manufacturer", connect = TRUE, shade = TRUE, scale = "percent")
document()
rm(list = c("ranked_dotplot"))
document()
devtools::document()   # rebuild Rd files from roxygen2
devtools::load_all()   # load package locally
devtools::check()      # full CRAN-like check
devtools::load_all()   # load package locally
devtools::check()      # full CRAN-like check
usethis::use_data(categories, mpg, starwars, overwrite = TRUE)
devtools::build_vignettes()# build vignettes, after changing anything in the markdown of the function
check()
roxygenize()
roxygenize()# Generate documentation
i
devtools::document()
